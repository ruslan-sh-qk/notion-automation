{
  "dependencies": {
    "@notionhq/client": "^3.1.3",
    "jest": "^30.0.0"
  },
  "scripts": {
    "test": "jest",
    "push": "git add . && git commit -m 'feature(TT-2)' && git push origin"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14"
  }
}
